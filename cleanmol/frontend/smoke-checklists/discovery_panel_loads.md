# Discovery Panel Smoke Checklist

- Dataset mode has `Auto-create` and `Chemist upload`.
- `Run Synthetic Demo` is visible and requires only an output folder.
- `Run Discovery` is disabled until required fields and warning acknowledgement are complete.
- Reduced-egress mode disables public HF/API source pulling.
