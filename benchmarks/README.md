# CleanMol Benchmarks

These lightweight benchmark fixtures support public-review sanity checks. They are not a validation claim.

Run:

```bash
python -m cleanmol.benchmarks.run_all
```

Current checks cover known disinfectant-like scaffolds, lower-priority controls, invalid SMILES handling, and ranking sanity behavior.
